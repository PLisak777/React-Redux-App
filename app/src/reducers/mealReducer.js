const initialState = {
	meals: {
		strMeal: '',
		strMealThumb: '',
		strInstructions: '',
	},
	is_loading_data: false,
	error: '',
};

export const mealReducer = (state = initialState, action) => {
switch(action.type) {
    default: return state
}
}
